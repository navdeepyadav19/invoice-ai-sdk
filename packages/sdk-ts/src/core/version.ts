/** The SDK's own version (semver). Keep in sync with package.json; a test checks. */
export const VERSION = '0.1.0'

/** The OpenAPI `info.version` this SDK was generated from, sent as `Invoice-AI-Version`. */
export { API_VERSION } from '../generated/meta'
